Flask>=3.1
Flask-SQLAlchemy>=3.0.3
Flask-CORS>=4.0.0
psycopg[binary]
python-dotenv>=1.0.0
Flask-Migrate>=4.0.4
Flask-Limiter>=3.5.0
PyJWT>=2.8.0
gunicorn>=21.2.0
twilio>=9.0.0
# Phase 4E (task 14.1) — Pydantic v2 powers the per-request body schemas
# in ``server/app/schemas/`` and the ``@validate_request`` decorator
# (task 14.2). Pinned to v2.x because every schema relies on
# ``ConfigDict(extra='forbid', strict=True)`` — Pydantic v1 has a
# different config surface and would silently ignore both flags.
pydantic>=2.0,<3.0
bcrypt>=4.0.0
cryptography>=42.0.0
redis>=5.0.0