[pytest]
# Only collect the real test suite under server/tests/. This prevents
# stray, ad-hoc `test_*.py` scripts placed in the server/ root (manual
# debug scripts that hit a live server or the DB at import time) from
# being auto-collected and crashing the CI run during collection.
testpaths = tests
